# Dependencies
node_modules/

# npm package lock is fine to keep, but local-only artifacts below are not:
*.log
npm-debug.log*

# Editor / OS cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local install artifacts (never commit a real install)
.zoom-in/
